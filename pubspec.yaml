name: your_app_name
description: A Flutter application with Firebase authentication.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12
  google_sign_in: ^7.2.0

  # Connectivity & Internet
  internet_connection_checker: ^1.0.0+1

  # Date & Time Formatting
  intl: ^0.18.1

  # UI
  cupertino_icons: ^1.0.2
  flutter_map: ^8.2.1
  location: ^8.0.1
  latlong2: ^0.9.1

  # State Management & Storage (NEW)
  provider: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true